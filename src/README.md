# Source structure

🚧 In Development — Not Ready for Testing

- `components/` — reusable UI components
- `lib/` — shared utilities and non-UI logic
- `data/` — frontend-safe static data and view models
- `main.jsx` — application entry point
- `styles.css` — global styles and Tailwind entry

Keep scoring and domain logic out of presentation components as the project grows.
