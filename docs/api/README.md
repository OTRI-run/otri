# API documentation

The API implementation lives in [`api/`](../../api) (FastAPI) — see [`api/README.md`](../../api/README.md) for the current endpoints, how to run it, and known gaps (no database, no auth yet).

This directory is reserved for future versioning policy, stability guarantees, and public API contract documentation once the API is stable enough to expose externally.
